import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.green.starter',
  appName: 'ionic-first',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
